
&НаКлиенте
Процедура Отмена(Команда)
	
	Закрыть();
	
КонецПроцедуры

&НаКлиенте
Процедура Заполнить(Команда)
	
	Если НЕ ЗначениеЗаполнено(Период) Тогда 
		Сообщить ("Необходимо заполнить период!");
		Возврат;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Показатель) Тогда 
		Сообщить("Необходимо заполнить показатель!");
		Возврат;
	КонецЕсли;
			
	Закрыть(Новый Структура ("Период,Показатель,Значение",Период,Показатель,ОбщаяСумма));
	
КонецПроцедуры
