
Процедура ПриКопировании(ОбъектКопирования)
	ИмяДляЗапросов = "";
КонецПроцедуры

Процедура ПередЗаписью(Отказ)
	Если Не ЗначениеЗаполнено(ИмяДляЗапросов) тогда
		ИмяДляЗапросов = СтрЗаменить(Наименование, " -", "");
	КонецЕсли;
	
	Если РаспределятьПланПоСменам И НЕ ЗначениеЗаполнено(ТипСменДляРаспределения) Тогда
		Сообщить("Не заполнен тип смен для распределения");	
		Отказ = Истина;
	КонецЕсли;
КонецПроцедуры
