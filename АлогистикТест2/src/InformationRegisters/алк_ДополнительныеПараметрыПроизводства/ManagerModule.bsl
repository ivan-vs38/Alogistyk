
Процедура ЗаписатьДопПараметр(ИмяПараметра, ПроизводствоСсылка, ЗначениеПараметра) Экспорт 
	
	Запись = РегистрыСведений.алк_ДополнительныеПараметрыПроизводства.СоздатьМенеджерЗаписи();
	Запись.Производство	= ПроизводствоСсылка;
	Запись.Прочитать();	
	
	Запись[ИмяПараметра] = ЗначениеПараметра;
	
	Если НЕ Запись.Выбран() Тогда	
		Запись.Производство	= ПроизводствоСсылка;				
	КонецЕсли;
	
	Запись.Записать();	

КонецПроцедуры